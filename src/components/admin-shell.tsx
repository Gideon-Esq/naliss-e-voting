"use client";
import Link from "next/link";import Image from"next/image";import{usePathname,useRouter}from"next/navigation";import{BarChart3,Bell,BookOpen,ClipboardPenLine,LayoutDashboard,LogOut,Settings,Upload}from"lucide-react";
const links=[["/admin","Dashboard",LayoutDashboard],["/admin/nominations","Candidate Links",ClipboardPenLine],["/admin/electorates","Electorates",Upload],["/admin/settings","Election Settings",Settings],["/admin/results","Vote Monitor / Results",BarChart3],["/admin/announcements","Announcements / Blog",BookOpen]]as const;
export function AdminShell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();async function logout(){await fetch("/api/admin/logout",{method:"POST"});router.push("/admin/login");router.refresh()}return <div className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><Image src="/naliss-logo.png" width={58} height={58} alt="NALISS emblem"/><span><b>NALISS</b><small>Electoral Commission</small></span></div><small className="menu-label">MAIN MENU</small>{links.map(([href,label,Icon])=><Link className={path===href||href!=="/admin"&&path.startsWith(href)?"active":""}href={href}key={href}><Icon/>{label}</Link>)}<button onClick={logout}><LogOut/>Logout</button></aside><section className="admin-main"><header><span>NALISS Electoral Commission　›　<b>{links.find(([href])=>path===href||href!=="/admin"&&path.startsWith(href))?.[1]??"Dashboard"}</b></span><div><Bell/><span className="admin-avatar">AU</span><b>Admin User<small>Administrator</small></b></div></header>{children}</section></div>}
