import{NextResponse}from"next/server";import{revalidatePath}from"next/cache";import{z}from"zod";import{isAdminAuthenticated}from"@/lib/admin-auth";import{db}from"@/lib/db";
const schema=z.object({title:z.string().trim().min(3).max(180),body:z.string().trim().min(10).max(10000),category:z.string().trim().min(2).max(60),status:z.enum(["DRAFT","PUBLISHED"]),featuredImage:z.string().max(7000000).nullable().optional()});
export async function POST(request:Request){if(!await isAdminAuthenticated())return NextResponse.json({message:"Unauthorized"},{status:401});const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({message:"Complete the post title and content."},{status:400});const election=await db.election.findFirst({where:{status:"PUBLISHED"}});const post=await db.announcement.create({data:{...parsed.data,electionId:election?.id,publishedAt:parsed.data.status==="PUBLISHED"?new Date():null}});revalidatePath("/announcements");revalidatePath("/admin/announcements");return NextResponse.json(post,{status:201})}
export async function DELETE(request:Request){if(!await isAdminAuthenticated())return NextResponse.json({message:"Unauthorized"},{status:401});const id=new URL(request.url).searchParams.get("id");if(!id)return NextResponse.json({message:"Missing post id."},{status:400});await db.announcement.delete({where:{id}});revalidatePath("/announcements");revalidatePath("/admin/announcements");return NextResponse.json({ok:true})}
